<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- 이모지 PNG 자체가 원형이라 배경색은 투명으로 둠 -->
    <solid android:color="@android:color/transparent" />
    <!-- 선택 표시용 얇은 테두리만 유지 -->
    <stroke
        android:width="1dp"
        android:color="#B2D0B0" />
</shape>
