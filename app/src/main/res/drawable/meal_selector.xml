<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state: Green border + light green background -->
    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/selected_green_bg"/>
            <stroke android:width="2dp" android:color="@color/selected_green_border"/>
            <corners android:radius="20dp"/>
        </shape>
    </item>
    <!-- Unselected state: Subtle white background + light beige border -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/white"/>
            <stroke android:width="1dp" android:color="@color/accent_beige"/>
            <corners android:radius="20dp"/>
        </shape>
    </item>
</selector>
